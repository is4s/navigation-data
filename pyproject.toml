[project]
name = "pntos-cobra-docs-data"
version = "1.0.0"
description = "Assets to build Cobra documentation."
authors = [{ name = "IS4S", email = "pntos@is4s.com" }]
dependencies = []
readme = "README.md"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.sdist]
include = ["branding/**"]

[tool.hatch.build.targets.wheel]
include = ["branding/**"]
